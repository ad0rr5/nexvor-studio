# Apache Güvenlik Yapılandırması
# Bu dosya Apache sunucusunda güvenlik ayarlarını yönetir

# Dizin listelerini devre dışı bırak
Options -Indexes

# Hassas dosyalara erişimi engelle
<FilesMatch "\.(env|log|config|backup|json|md)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# Node.js ve geliştirme dosyalarına erişimi engelle
<FilesMatch "(package\.json|package-lock\.json|tsconfig\.json|vite\.config\.|\.gitignore)">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# Kaynak kod dosyalarına erişimi engelle
<FilesMatch "\.(tsx?|jsx?)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# .git dizinine erişimi engelle
RedirectMatch 404 /\.git

# Güvenlik başlıkları
<IfModule mod_headers.c>
    Header always set X-Frame-Options "SAMEORIGIN"
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-XSS-Protection "1; mode=block"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    Header always set Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'"
</IfModule>

# MIME type güvenliği
<IfModule mod_mime.c>
    AddType text/html .html
    AddType text/css .css
    AddType application/javascript .js
</IfModule>
